plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

application {
    mainClass = 'DepartmentalServer' // 👈 Cambia esto si tu clase main se llama distinto
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}